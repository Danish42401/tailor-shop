# ── PUBLIC (client-safe — these are exposed in the browser) ─────────────────
NEXT_PUBLIC_SITE_URL=https://tailor-shop.vercel.app
NEXT_PUBLIC_WHATSAPP_NUMBER=971562392496
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=dyxnglxdj
NEXT_PUBLIC_GA_MEASUREMENT_ID=               # Leave empty — add after site is live

# ── PRIVATE (server-only — never expose in client code) ─────────────────────
RESEND_API_KEY=your_resend_api_key_here        # Get from https://resend.com/api-keys
OWNER_EMAIL=your_email@example.com              # Owner email to receive order notifications
FROM_EMAIL=onboarding@resend.dev             # Use this until custom domain verified
DAILY_SUMMARY_SECRET=tailor_shop_daily_2024  # Change this to a strong random secret
